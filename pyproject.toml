[project]
name = "transporter"
version = "0.1.0"
description = "Keyboard teleoperation for LeKiwi wheels"
requires-python = ">=3.10"
dependencies = [
    "lerobot[feetech]>=0.4.2",
]

[tool.uv]
package = false
